import styled from 'styled-components';
import { variables } from '../../shared/styles';

export const Button = styled.button`
  padding: 12px 16px;
  background-color: ${variables.colors.gray.base};
  border-radius: 4px;
`;
